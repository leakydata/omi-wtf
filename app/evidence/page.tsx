import { evidence } from "../data"; import { Disclaimer, EvidenceCard, Footer, Header, PageIntro } from "../site-shell";
export default function EvidencePage() { return <><main><Header /><PageIntro kicker="THE RECEIPTS" title="Don’t take my word for it"><p>A company can dismiss one angry email. It is harder to dismiss its own pages, its own support messages, and its own refund notice. The public copies below remove private details, not inconvenient facts.</p></PageIntro><div className="page-body"><Disclaimer /><div className="evidence-key"><span><i className="dot verified" /> Public file checked</span><span><i className="dot pending" /> File not public yet</span><span><i className="dot account" /> Personal recollection</span><span><i className="dot unresolved" /> Still unresolved</span></div><section className="evidence-grid">{evidence.map(item => <EvidenceCard item={item} key={item.id} />)}</section></div></main><Footer /></>; }
