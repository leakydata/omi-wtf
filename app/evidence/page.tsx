import { evidence } from "../data"; import { Disclaimer, EvidenceCard, Footer, Header, PageIntro } from "../site-shell";
export default function EvidencePage() { return <><main><Header /><PageIntro kicker="THE RECEIPTS" title="Don’t take my word for it"><p>A company can dismiss one angry email. It is harder to dismiss its own pages, support messages, and refund notice. I removed private details from the copies below. I did not remove facts that make Omi look bad.</p></PageIntro><div className="page-body"><Disclaimer /><div className="evidence-key"><span><i className="dot verified" /> Source checked</span><span><i className="dot pending" /> File not online yet</span><span><i className="dot account" /> What I remember</span><span><i className="dot unresolved" /> Still checking</span></div><section className="evidence-grid">{evidence.map(item => <EvidenceCard item={item} key={item.id} />)}</section></div></main><Footer /></>; }
