import { timeline } from "../data"; import { Disclaimer, Footer, Header, PageIntro } from "../site-shell";
export default function TimelinePage() { return <><main><Header /><PageIntro kicker="WHILE THE PACKAGE WAS MOVING" title="How the deal changed"><p>I ordered on August 12. Before delivery, a higher price appeared. Then the free allowance changed, support contradicted the published pages, and the entire order was refunded. Follow it record by record.</p></PageIntro><div className="page-body"><Disclaimer /><section className="full-timeline">{timeline.map(([date, title, text, id], i) => <article key={`${date}-${title}`}><span className="sequence">{String(i + 1).padStart(2, "0")}</span><time>{date}, 2026</time><div><h2>{title}</h2><p>{text}</p><a href={`/evidence#${id}`}>{id} ↗</a></div></article>)}</section></div></main><Footer /></>; }
