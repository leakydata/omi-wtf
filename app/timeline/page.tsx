import { timeline } from "../data"; import { Disclaimer, Footer, Header, PageIntro } from "../site-shell";
export default function TimelinePage() { return <><main><Header /><PageIntro kicker="WHILE THE PACKAGE WAS MOVING" title="How the deal changed"><p>I ordered on August 12. Before delivery, a higher price appeared. Then the number of free minutes changed. Support said things that did not match Omi’s pages, and Omi refunded the whole order. Here is what happened, one record at a time.</p></PageIntro><div className="page-body"><Disclaimer /><section className="full-timeline">{timeline.map(([date, title, text, id], i) => <article key={`${date}-${title}`}><span className="sequence">{String(i + 1).padStart(2, "0")}</span><time>{date}, 2026</time><div><h2>{title}</h2><p>{text}</p><a href={`/evidence#${id}`}>{id} ↗</a></div></article>)}</section></div></main><Footer /></>; }
