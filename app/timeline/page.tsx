import { timeline } from "../data"; import { Disclaimer, Footer, Header, PageIntro } from "../site-shell";
export default function TimelinePage() { return <><main><Header /><PageIntro kicker="CHRONOLOGY" title="The documented timeline"><p>From purchase through refund, each event is tied to an indexed record. An evidence ID does not mean the underlying file is public yet; archive status is shown in the evidence index.</p></PageIntro><div className="page-body"><Disclaimer /><section className="full-timeline">{timeline.map(([date, title, text, id], i) => <article key={`${date}-${title}`}><span className="sequence">{String(i + 1).padStart(2, "0")}</span><time>{date}, 2026</time><div><h2>{title}</h2><p>{text}</p><a href={`/evidence#${id}`}>{id} ↗</a></div></article>)}</section></div></main><Footer /></>; }
