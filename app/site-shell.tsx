import { nav, type EvidenceItem } from "./data";
export function Header() { return <><div className="topline"><span>Independent consumer documentation</span><span>Last updated August 23, 2026</span></div><header className="site-header"><a className="wordmark" href="/" aria-label="Omi pricing record home"><span>OMI</span><strong>PRICING RECORD</strong></a><nav aria-label="Primary navigation">{nav.slice(0, 5).map(([label, href]) => <a key={href} href={href}>{label}</a>)}<a className="nav-cta" href="/experiences">Share your experience</a></nav></header></>; }
export function Footer() { return <footer><div><a className="wordmark inverse" href="/"><span>OMI</span><strong>PRICING RECORD</strong></a><p>An independent, evidence-led record of a consumer pricing dispute.</p></div><div className="footer-links">{nav.map(([label, href]) => <a key={href} href={href}>{label}</a>)}<a href="/legal">Legal issues</a><a href="/experiences">Share your experience</a></div><p className="footer-note">This site is independently operated and is not affiliated with, endorsed by, or operated by Omi or Based Hardware, Inc. It provides general information, not legal advice.</p></footer>; }
export function PageIntro({ kicker, title, children }: { kicker: string; title: string; children: React.ReactNode }) { return <section className="page-intro"><span className="kicker">{kicker}</span><h1>{title}</h1><div className="intro-copy">{children}</div></section>; }
export function EvidenceCard({ item }: { item: EvidenceItem }) { return <article className="evidence-card" id={item.id}><div className="evidence-card-head"><span>{item.category}</span><span className={`status ${item.status.toLowerCase().replaceAll(" ", "-")}`}>{item.status}</span></div><h3>{item.title}</h3><p>{item.summary}</p><dl><div><dt>Evidence ID</dt><dd>{item.id}</dd></div><div><dt>Date</dt><dd>{item.date}</dd></div><div><dt>Source</dt><dd>{item.source}</dd></div></dl><div className="file-state">{item.publicFile && item.publicPath ? <a href={item.publicPath} target="_blank" rel="noreferrer">View redacted public PDF ↗</a> : "Supporting file has not yet been added to the public archive."}</div></article>; }
export function Quote({ children, cite, id }: { children: React.ReactNode; cite: string; id: string }) { return <figure className="quote"><blockquote>“{children}”</blockquote><figcaption><span>{cite}</span><a href={`/evidence#${id}`}>{id} ↗</a></figcaption></figure>; }
export function Disclaimer() { return <aside className="disclaimer"><strong>How to read this record</strong><p>Documented facts, quotations, first-person recollections, and unresolved questions are labeled separately. Missing public files are stated plainly. No allegation is presented as an adjudicated fact.</p></aside>; }
