import { nav, type EvidenceItem } from "./data";
export function Header() { return <><div className="topline"><span>Built by a customer. Backed by records. Not Omi.</span><span>Last updated August 23, 2026</span></div><header className="site-header"><a className="wordmark" href="/" aria-label="Omi pricing record home"><span>OMI</span><strong>PRICING RECORD</strong></a><nav aria-label="Primary navigation">{nav.slice(0, 7).map(([label, href]) => <a className="desktop-nav-link" key={href} href={href}>{label}</a>)}<a className="nav-cta" href="/experiences">Did this happen to you?</a><details className="mobile-menu"><summary>Menu</summary><div className="mobile-menu-links">{nav.map(([label, href]) => <a key={href} href={href}>{label}</a>)}</div></details></nav></header></>; }
export function Footer() { return <footer><div><a className="wordmark inverse" href="/"><span>OMI</span><strong>PRICING RECORD</strong></a><p>Built by a customer who kept the receipts. You buy the device once. Omi should not gain more power to raise prices as your saved history grows.</p></div><div className="footer-links">{nav.map(([label, href]) => <a key={href} href={href}>{label}</a>)}<a href="/legal">Why this matters</a><a href="/experiences">Tell your story</a></div><p className="footer-note">A customer runs this site. Omi does not run or approve it. This site shares records and opinions. It is not legal advice.</p></footer>; }
export function PageIntro({ kicker, title, children }: { kicker: string; title: string; children: React.ReactNode }) { return <section className="page-intro"><span className="kicker">{kicker}</span><h1>{title}</h1><div className="intro-copy">{children}</div></section>; }
export function EvidenceCard({ item }: { item: EvidenceItem }) { return <article className="evidence-card" id={item.id}><div className="evidence-card-head"><span>{item.category}</span><span className={`status ${item.status.toLowerCase().replaceAll(" ", "-")}`}>{item.status}</span></div><h3>{item.title}</h3><p>{item.summary}</p><dl><div><dt>Record ID</dt><dd>{item.id}</dd></div><div><dt>Date</dt><dd>{item.date}</dd></div><div><dt>Comes from</dt><dd>{item.source}</dd></div></dl><div className="file-state">{item.publicFile && item.publicPath ? <a href={item.publicPath} target="_blank" rel="noreferrer">{item.publicLabel ?? "Open the copy with private details removed ↗"}</a> : item.fileNote ?? "The supporting file is not online yet."}</div></article>; }
export function Quote({ children, cite, id }: { children: React.ReactNode; cite: string; id: string }) { return <figure className="quote"><blockquote>&quot;{children}&quot;</blockquote><figcaption><span>{cite}</span><a href={`/evidence#${id}`}>{id} ↗</a></figcaption></figure>; }
export function Disclaimer() { return <aside className="disclaimer"><strong>Records, firsthand accounts, and reasonable conclusions</strong><p>The records are linked. Events I personally saw or heard are labeled. From there, I say what the facts reasonably show.</p></aside>; }
