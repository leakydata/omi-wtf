import { nav, type EvidenceItem } from "./data";
export function Header() { return <><div className="topline"><span>Customer-built. Evidence-backed. Not Omi.</span><span>Last updated August 23, 2026</span></div><header className="site-header"><a className="wordmark" href="/" aria-label="Omi pricing record home"><span>OMI</span><strong>PRICING RECORD</strong></a><nav aria-label="Primary navigation">{nav.slice(0, 5).map(([label, href]) => <a key={href} href={href}>{label}</a>)}<a className="nav-cta" href="/experiences">Did this happen to you?</a></nav></header></>; }
export function Footer() { return <footer><div><a className="wordmark inverse" href="/"><span>OMI</span><strong>PRICING RECORD</strong></a><p>Built by a customer who kept the receipts. Hardware is a one-time purchase. Your memory archive should not become pricing leverage.</p></div><div className="footer-links">{nav.map(([label, href]) => <a key={href} href={href}>{label}</a>)}<a href="/legal">Why this matters</a><a href="/experiences">Tell your story</a></div><p className="footer-note">This is independent commentary and consumer documentation. It is not an Omi website and it is not legal advice.</p></footer>; }
export function PageIntro({ kicker, title, children }: { kicker: string; title: string; children: React.ReactNode }) { return <section className="page-intro"><span className="kicker">{kicker}</span><h1>{title}</h1><div className="intro-copy">{children}</div></section>; }
export function EvidenceCard({ item }: { item: EvidenceItem }) { return <article className="evidence-card" id={item.id}><div className="evidence-card-head"><span>{item.category}</span><span className={`status ${item.status.toLowerCase().replaceAll(" ", "-")}`}>{item.status}</span></div><h3>{item.title}</h3><p>{item.summary}</p><dl><div><dt>Evidence ID</dt><dd>{item.id}</dd></div><div><dt>Date</dt><dd>{item.date}</dd></div><div><dt>Source</dt><dd>{item.source}</dd></div></dl><div className="file-state">{item.publicFile && item.publicPath ? <a href={item.publicPath} target="_blank" rel="noreferrer">{item.publicLabel ?? "Open the redacted file ↗"}</a> : "This supporting file is not public yet."}</div></article>; }
export function Quote({ children, cite, id }: { children: React.ReactNode; cite: string; id: string }) { return <figure className="quote"><blockquote>“{children}”</blockquote><figcaption><span>{cite}</span><a href={`/evidence#${id}`}>{id} ↗</a></figcaption></figure>; }
export function Disclaimer() { return <aside className="disclaimer"><strong>Receipts, recollections, and open questions</strong><p>Documents are linked. Personal recollections are labeled. Open questions stay open. You can disagree with my conclusion without having to guess what the evidence says.</p></aside>; }
